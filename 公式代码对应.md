# 公式与代码对应解读

**论文来源**：A new family of Constitutive Artificial Neural Networks towards automated model discovery  
**作者**：Kevin Linka, Ellen Kuhl（Stanford University）  
**代码仓库**：https://github.com/marksweli/CANN

本文档按论文章节顺序，将论文中所有出现的公式与仓库中最底层的计算代码一一对应，以"代码在前、公式在后"的格式呈现。所有公式均采用 LaTeX 行内或行间渲染方式。

---

## 2. Kinematics（运动学）

---

### 公式 (1)：变形梯度与雅可比行列式

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L80-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 80–87 行
# 对单轴拉伸（UT）计算 I1、I2，其中 Stretch = λ = F_{11}
# 对应 F = diag{λ, λ^{-1/2}, λ^{-1/2}}，J = det(F) = 1

I1_UT = keras.layers.Lambda(lambda x: x**2   + 2.0/x  )(Stretch_UT)
I2_UT = keras.layers.Lambda(lambda x: 2.0*x  + 1/x**2 )(Stretch_UT)
# 纯剪切 PS：F = diag{λ, 1, λ^{-1}}，J = 1
I1_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2 )(Stretch_PS)
I2_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2 )(Stretch_PS)
# 等双轴拉伸 ET：F = diag{λ, λ, λ^{-2}}，J = 1
I1_ET = keras.layers.Lambda(lambda x: 2.0*x**2 + 1.0/x**4 )(Stretch_ET)
I2_ET = keras.layers.Lambda(lambda x:     x**4 + 2.0/x**2 )(Stretch_ET)
```

代码中以单轴拉伸主拉伸 $\lambda$ 作为变形梯度 $\boldsymbol{F}$ 的分量直接输入，并利用不可压缩条件 $J = \det(\boldsymbol{F}) = 1$ 显式将 $J$ 消去，从而将整个运动学描述归结为主拉伸 $\lambda$。

**公式 (1)**：

$$
\boldsymbol{F} = \nabla_{\boldsymbol{X}}\phi \qquad \text{with} \qquad J = \det(\boldsymbol{F}) > 0
$$

---

### 公式 (2)：右、左 Cauchy–Green 变形张量

**对应代码**（[GCANN/src/CANN/cont_mech.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/cont_mech.py#L46-L65)）：

```python
# GCANN/src/CANN/cont_mech.py，第 46–65 行
# 对 0-90 方向的双轴拉伸计算不变量，直接体现 C = F^t·F 的迹结构
def get_inv_value_biax_90(stretch):
    Is = np.zeros((stretch[0].shape[0], 8))
    Stretch_w = stretch[0]
    Stretch_s = stretch[1]
    Stretch_z = 1. / (Stretch_w * Stretch_s)   # 不可压缩条件：λ_w λ_s λ_z = 1

    # I1 = tr(C) - 3 = λ_w² + λ_s² + λ_z² - 3
    Is[:, 0] = Stretch_w ** 2 + Stretch_s ** 2 + Stretch_z ** 2 - 3
    # I2 = (1/2)(I1² - tr(C²)) - 3 = λ_w^{-2} + λ_s^{-2} + λ_z^{-2} - 3
    Is[:, 1] = Stretch_w ** (-2) + Stretch_s ** (-2) + Stretch_z ** (-2) - 3
```

代码利用主拉伸计算 $\operatorname{tr}(\boldsymbol{C}) = \lambda_w^2 + \lambda_s^2 + \lambda_z^2$，本质上是在主拉伸空间中计算右 Cauchy–Green 张量 $\boldsymbol{C} = \boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}$ 的迹。

**公式 (2)**：

$$
\boldsymbol{C} = \boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F} \qquad \text{and} \qquad \boldsymbol{b} = \boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}
$$

---

### 公式 (3)：以变形梯度 $\boldsymbol{F}$ 表达的三个主不变量

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L79-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 79–87 行
# 对完全不可压缩材料（I3 = J² = 1），三个不变量退化为两个
# I1 = F:F（对 UT）= λ² + 2/λ
I1_UT = keras.layers.Lambda(lambda x: x**2   + 2.0/x  )(Stretch_UT)
# I2 = (1/2)[I1² - (F^t·F):(F^t·F)]（对 UT）= 2λ + 1/λ²
I2_UT = keras.layers.Lambda(lambda x: 2.0*x  + 1/x**2 )(Stretch_UT)
# I3 = det(F^t·F) = J² = 1（完全不可压缩，在代码中隐含）
```

**公式 (3)**：

$$
\begin{array}{lll}
I_1 = \boldsymbol{F}:\boldsymbol{F} & \partial_{\boldsymbol{F}}I_1 = 2\boldsymbol{F}\\
I_2 = \dfrac{1}{2}\!\left[I_1^2 - [\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}]:[\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}]\right] & \text{with}\quad \partial_{\boldsymbol{F}}I_2 = 2[I_1\boldsymbol{F} - \boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}]\\
I_3 = \det(\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}) = J^2 & \partial_{\boldsymbol{F}}I_3 = 2I_3\boldsymbol{F}^{-\mathrm{t}}
\end{array}
$$

---

### 公式 (4)：以 $\boldsymbol{C}$ 或 $\boldsymbol{b}$ 表达的主不变量

**对应代码**（[GCANN/src/CANN/cont_mech.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/cont_mech.py#L46-L87)）：

```python
# GCANN/src/CANN/cont_mech.py，第 46–87 行
# 不变量以主拉伸（即 C 的特征值）形式计算

def get_inv_value_biax_90(stretch):
    Stretch_w = stretch[0]
    Stretch_s = stretch[1]
    Stretch_z = 1. / (Stretch_w * Stretch_s)
    # I1 = tr(C) - 3
    Is[:, 0] = Stretch_w ** 2 + Stretch_s ** 2 + Stretch_z ** 2 - 3
    # I2 = (1/2)[I1² - C:C] - 3 = λ_w^{-2} + λ_s^{-2} + λ_z^{-2} - 3
    Is[:, 1] = Stretch_w ** (-2) + Stretch_s ** (-2) + Stretch_z ** (-2) - 3

def get_inv_value_biax_45(stretch):
    Stretch_x = stretch[0]
    Stretch_y = stretch[1]
    Stretch_z = 1. / (Stretch_x * Stretch_y)
    # 同样以主拉伸计算 I1, I2
    Is[:, 0] = Stretch_x ** 2 + Stretch_y ** 2 + Stretch_z ** 2 - 3
    Is[:, 1] = Stretch_x ** (-2) + Stretch_y ** (-2) + Stretch_z ** (-2) - 3
```

**公式 (4)**：

$$
\begin{array}{llll}
I_1 = \mathrm{tr}(\boldsymbol{C}) = \boldsymbol{C}:\boldsymbol{I} & \partial_{\boldsymbol{C}}I_1 = \boldsymbol{I} & I_1 = \mathrm{tr}(\boldsymbol{b}) = \boldsymbol{b}:\boldsymbol{I} & \partial_{\boldsymbol{b}}I_1 = \boldsymbol{I}\\
I_2 = \tfrac{1}{2}[I_1^2 - \boldsymbol{C}:\boldsymbol{C}] & \partial_{\boldsymbol{C}}I_2 = I_1\boldsymbol{I} - \boldsymbol{C} & \text{or}\quad I_2 = \tfrac{1}{2}[I_1^2 - \boldsymbol{b}:\boldsymbol{b}] & \partial_{\boldsymbol{b}}I_2 = I_1\boldsymbol{I} - \boldsymbol{b}\\
I_3 = \det(\boldsymbol{C}) = J^2 & \partial_{\boldsymbol{C}}I_3 = I_3\boldsymbol{C}^{-1} & I_3 = \det(\boldsymbol{b}) = J^2 & \partial_{\boldsymbol{b}}I_3 = I_3\boldsymbol{b}^{-1}
\end{array}
$$

---

### 公式 (5)：变形梯度的体积-等容分解

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L80-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 80–87 行
# 对完全不可压缩材料，J = 1，因此 F̄ = J^{-1/3} F = F
# 代码直接以 Stretch_UT（即 λ）作为不变量计算的基础，隐含 J = 1
I1_UT = keras.layers.Lambda(lambda x: x**2   + 2.0/x  )(Stretch_UT)
I2_UT = keras.layers.Lambda(lambda x: 2.0*x  + 1/x**2 )(Stretch_UT)

# GCANN/src/CANN/cont_mech.py，第 56 行
# 等容约束由以下方式显式施加
Stretch_z = 1. / (Stretch_w * Stretch_s)   # λ_z = 1/(λ_w · λ_s)，保证 J̄ = 1
```

**公式 (5)**：

$$
\bar{\boldsymbol{F}} = J^{-1/3}\boldsymbol{F} \qquad \text{and} \qquad \bar{J} = \det(\bar{\boldsymbol{F}}) = 1
$$

---

### 公式 (6)：等容右、左 Cauchy–Green 张量

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L80-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 80–87 行
# 对完全不可压缩材料，J = 1，C̃ = J^{-2/3} C = C，b̃ = J^{-2/3} b = b
# 因此代码中的 I1_UT 等价于用 C̃ 计算
I1_UT = keras.layers.Lambda(lambda x: x**2   + 2.0/x  )(Stretch_UT)   # I1 = tr(C̃) = λ² + 2/λ
I2_UT = keras.layers.Lambda(lambda x: 2.0*x  + 1/x**2 )(Stretch_UT)   # I2 = tr(C̃⁻¹) = 2λ + 1/λ²
```

**公式 (6)**：

$$
\tilde{\boldsymbol{C}} = \tilde{\boldsymbol{F}}^{\mathrm{t}}\cdot\tilde{\boldsymbol{F}} = J^{-2/3}\boldsymbol{C} \qquad \text{and} \qquad \tilde{\boldsymbol{b}} = \tilde{\boldsymbol{F}}\cdot\tilde{\boldsymbol{F}}^{\mathrm{t}} = J^{-2/3}\boldsymbol{b}
$$

---

### 公式 (7)：以 $\tilde{\boldsymbol{F}}$ 表达的等容不变量及其梯度

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L80-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 80–87 行
# 对不可压缩材料（J = 1），等容不变量与经典不变量相同
# Ĩ1 = I1 / J^{2/3} = I1（当 J=1 时）
I1_UT = keras.layers.Lambda(lambda x: x**2 + 2.0/x)(Stretch_UT)      # Ĩ1 for UT
I2_UT = keras.layers.Lambda(lambda x: 2.0*x + 1/x**2)(Stretch_UT)    # Ĩ2 for UT
I1_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)   # Ĩ1 for PS
I2_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)   # Ĩ2 for PS
I1_ET = keras.layers.Lambda(lambda x: 2.0*x**2 + 1.0/x**4)(Stretch_ET)     # Ĩ1 for ET
I2_ET = keras.layers.Lambda(lambda x:     x**4 + 2.0/x**2)(Stretch_ET)     # Ĩ2 for ET
```

**公式 (7)**：

$$
\begin{array}{rcl}
\tilde{I}_1 &=& I_1/J^{2/3} = \boldsymbol{F}:\boldsymbol{F}/J^{2/3}\\
\tilde{I}_2 &=& I_2/J^{4/3} = \tfrac{1}{2}\!\left[\tilde{I}_1 - [\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}]:[\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}]/J^{4/3}\right]
\end{array}
\qquad
\begin{array}{rcl}
\partial_{\boldsymbol{F}}\tilde{I}_1 &=& 2/J^{2/3}\boldsymbol{F} - \tfrac{2}{3}\tilde{I}_1\boldsymbol{F}^{-\mathrm{t}}\\
\partial_{\boldsymbol{F}}\tilde{I}_2 &=& 2/J^{2/3}\tilde{I}_1\boldsymbol{F} - 2/J^{4/3}\boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F} - \tfrac{4}{3}\tilde{I}_2\boldsymbol{F}^{-\mathrm{t}}\\
\partial_{\boldsymbol{F}}J &=& J\boldsymbol{F}^{-\mathrm{t}}
\end{array}
$$

---

### 公式 (8)：以 $\boldsymbol{C}$ 或 $\boldsymbol{b}$ 表达的等容不变量

**对应代码**（[GCANN/src/CANN/cont_mech.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/cont_mech.py#L46-L65)）：

```python
# GCANN/src/CANN/cont_mech.py，第 46–65 行
# 完全不可压缩时 J = 1，以主拉伸计算等容不变量（与公式 (4) 等价）
def get_inv_value_biax_90(stretch):
    Stretch_w = stretch[0]
    Stretch_s = stretch[1]
    Stretch_z = 1. / (Stretch_w * Stretch_s)

    # Ĩ1 = I1/J^{2/3} = tr(C)/1 = tr(C) （J=1）
    Is[:, 0] = Stretch_w ** 2 + Stretch_s ** 2 + Stretch_z ** 2 - 3
    # Ĩ2 = I2/J^{4/3} = (1/2)(I1² - C:C)/1 （J=1）
    Is[:, 1] = Stretch_w ** (-2) + Stretch_s ** (-2) + Stretch_z ** (-2) - 3
```

**公式 (8)**：

$$
\begin{array}{rcl}
\tilde{I}_1 &=& I_1/J^{2/3} = \boldsymbol{C}:\boldsymbol{I}/J^{2/3}\\
\tilde{I}_2 &=& I_2/J^{4/3} = \tfrac{1}{2}[\tilde{I}_1 - \boldsymbol{C}:\boldsymbol{C}/J^{4/3}]\\
J &=& \det^{1/2}(\boldsymbol{C})
\end{array}
\qquad
\begin{array}{rcl}
\tilde{I}_1 &=& I_1/J^{2/3} = \boldsymbol{b}:\boldsymbol{I}/J^{2/3}\\
\tilde{I}_2 &=& I_2/J^{4/3} = \tfrac{1}{2}[\tilde{I}_1 - \boldsymbol{b}:\boldsymbol{I}/J^{4/3}]\\
J &=& \det^{1/2}(\boldsymbol{b})
\end{array}
$$

---

### 公式 (9)：横观各向同性的第四、五不变量

**对应代码**（[GCANN/src/CANN/cont_mech.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/cont_mech.py#L58-L65)）：

```python
# GCANN/src/CANN/cont_mech.py，第 58–65 行
# I4w = n · C · n = λ_w²（以纬线方向为主方向时）
Is[:, 2] = Stretch_w ** 2 - 1    # I4_w - 1（参考构型扣除）

# GCANN/src/CANN/cont_mech.py，第 81–86 行（45 度方向）
# I4w（45°方向）= (λ_x² + λ_y²)/2
Is[:, 2] = (Stretch_x ** 2 + Stretch_y ** 2) / 2 - 1
# I4s（45°方向）= 同上
Is[:, 4] = (Stretch_x ** 2 + Stretch_y ** 2) / 2 - 1
# I8ws（混合项）
Is[:, 6] = np.abs(Stretch_x ** 2 - Stretch_y ** 2) / 2
```

**公式 (9)**：

$$
\begin{array}{rcl}
I_4 &=& \boldsymbol{n}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}\cdot\boldsymbol{n}\\
I_5 &=& \boldsymbol{n}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F}\cdot\boldsymbol{n}
\end{array}
\qquad
\begin{array}{rcl}
\boldsymbol{n}\cdot\boldsymbol{N} = \lambda_n^2 && \text{with}\quad \partial_{\boldsymbol{C}}I_4 = \boldsymbol{n}\otimes\boldsymbol{n} = \boldsymbol{N}\\
\boldsymbol{C}\cdot\boldsymbol{C}:\boldsymbol{N} && \text{with}\quad \partial_{\boldsymbol{C}}I_5 = \boldsymbol{n}\otimes\boldsymbol{C}\cdot\boldsymbol{n} + \boldsymbol{C}\cdot\boldsymbol{n}\otimes\boldsymbol{n}
\end{array}
$$

---

## 3. Constitutive equations（本构方程）

---

### 公式 (10)：广义本构关系

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L72-L123)）：

```python
# RUBBER/ContinuumMechHelper.py，第 72–123 行
# ContinuumMechanicsFramework 函数将应变能模型 Psi_model 嵌入
# 并定义完整的 P(F) 映射（通过 I1, I2 → Psi → dPsi/dI → Stress）
def ContinuumMechanicsFramework(Psi_model):
    Stretch_UT = keras.layers.Input(shape=(1,), name='Stretch_UT')
    ...
    I1_UT = keras.layers.Lambda(lambda x: x**2 + 2.0/x)(Stretch_UT)
    I2_UT = keras.layers.Lambda(lambda x: 2.0*x + 1/x**2)(Stretch_UT)
    Psi_UT = Psi_model([I1_UT, I2_UT])         # 学习 ψ(F)
    ...
    Stress_UT = keras.layers.Lambda(function=Stress_calc_UT, ...)(...)   # 计算 P(F)
    model_UT = keras.models.Model(inputs=Stretch_UT, outputs=Stress_UT)  # P = P(F)
```

**公式 (10)**：

$$
\boldsymbol{P} = \boldsymbol{P}(\boldsymbol{F})
$$

---

### 公式 (11)：热力学一致性——超弹性势函数导出应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L26-L28)）：

```python
# RUBBER/ContinuumMechHelper.py，第 26–28 行
# myGradient 函数实现 P = ∂ψ/∂F 的自动微分计算
def myGradient(a, b):
    der = tf.gradients(a, b, unconnected_gradients='zero')
    return der[0]

# 第 95–102 行：对每种加载模式计算 ∂ψ/∂I1 和 ∂ψ/∂I2（链式法则）
dWdI1_UT  = keras.layers.Lambda(lambda x: myGradient(x[0], x[1]))([Psi_UT, I1_UT])
dWdI2_UT  = keras.layers.Lambda(lambda x: myGradient(x[0], x[1]))([Psi_UT, I2_UT])
```

**公式 (11)**：

$$
\boldsymbol{P} = \frac{\partial\psi(\boldsymbol{F})}{\partial\boldsymbol{F}}
$$

---

### 公式 (12)：材料客观性——以右 Cauchy–Green 张量导出应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L26-L41)）：

```python
# RUBBER/ContinuumMechHelper.py，第 26–41 行
# 以链式法则将 ∂ψ/∂F 分解为 ∂ψ/∂I1 · ∂I1/∂F + ∂ψ/∂I2 · ∂I2/∂F
# Stress_calc_UT 实现了 P = 2F · ∂ψ/∂C 在主拉伸坐标系下的具体形式
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    minus  = two * (dPsidI1 * one / K.square(Stretch) + dPsidI2 * one / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2 * one) - minus
    return stress
```

在主拉伸坐标中，$\partial\psi/\partial\boldsymbol{C}$ 通过链式法则转换为 $\partial\psi/\partial I_k \cdot \partial I_k/\partial\boldsymbol{C}$，再与 $2\boldsymbol{F}$ 相乘得到 $\boldsymbol{P}$。

**公式 (12)**：

$$
\boldsymbol{P} = \frac{\partial\psi(\boldsymbol{C})}{\partial\boldsymbol{F}} = \frac{\partial\psi(\boldsymbol{C})}{\partial\boldsymbol{C}}:\frac{\partial\boldsymbol{C}}{\partial\boldsymbol{F}} = 2\boldsymbol{F}\cdot\frac{\partial\psi(\boldsymbol{C})}{\partial\boldsymbol{C}}
$$

---

### 公式 (13)：各向同性材料的 Piola 应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L41)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–41 行
# 单轴拉伸应力计算
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    # 对应公式 (13) 中的三项之和在主拉伸坐标下的化简：
    # minus = 2(∂ψ/∂I1 · 1/λ² + ∂ψ/∂I2 · 1/λ³)  [源自 -2∂ψ/∂I2 · F·F^t·F 与 2I3∂ψ/∂I3 F^{-t} 项]
    minus  = two * (dPsidI1 * one / K.square(Stretch) + dPsidI2 * one / K.pow(Stretch, 3))
    # stress = 2(∂ψ/∂I1 + I1·∂ψ/∂I2)F 项
    stress = two * (dPsidI1 * Stretch + dPsidI2 * one) - minus
    return stress
```

**公式 (13)**：

$$
\boldsymbol{P} = \frac{\partial\psi(I_1,I_2,I_3)}{\partial\boldsymbol{F}} = 2\!\left[\frac{\partial\psi}{\partial I_1} + I_1\frac{\partial\psi}{\partial I_2}\right]\boldsymbol{F} - 2\frac{\partial\psi}{\partial I_2}\boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F} + 2I_3\frac{\partial\psi}{\partial I_3}\boldsymbol{F}^{-\mathrm{t}}
$$

---

### 公式 (14)：近似不可压缩材料的 Piola 应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L41)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–41 行
# 对完全不可压缩极限（J = 1），公式 (14) 退化为公式 (15)
# 代码直接实现的是完全不可压缩情形（J=1），与公式 (14) 的 J→1 极限完全一致
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    two = tf.constant(2.0, dtype='float32')
    minus  = two * (dPsidI1 / K.square(Stretch) + dPsidI2 / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2) - minus
    return stress
```

**公式 (14)**：

$$
\boldsymbol{P} = \frac{\partial\psi(\bar{I}_1,\bar{I}_2,J)}{\partial\boldsymbol{F}} = 2\frac{1}{J^{2/3}}\!\left[\frac{\partial\psi}{\partial\bar{I}_1} + \bar{I}_1\frac{\partial\psi}{\partial\bar{I}_2}\right]\boldsymbol{F} - 2\frac{1}{J^{4/3}}\frac{\partial\psi}{\partial\bar{I}_2}\boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F} - \frac{2}{3}\!\left[\bar{I}_1\frac{\partial\psi}{\partial\bar{I}_1} + 2\bar{I}_2\frac{\partial\psi}{\partial\bar{I}_2}\right]\boldsymbol{F}^{-\mathrm{t}} + J\frac{\partial\psi}{\partial J}\boldsymbol{F}^{-\mathrm{t}}
$$

---

### 公式 (15)：完全不可压缩材料的 Piola 应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L41)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–41 行
# 公式 (15) 是代码中应力计算的直接实现基础
# 在主拉伸坐标系下，pF^{-1} 项化简为静水压力贡献（minus 项）
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one  = tf.constant(1.0, dtype='float32')
    two  = tf.constant(2.0, dtype='float32')
    # P = 2(∂ψ/∂I1 + I1·∂ψ/∂I2)F - 2∂ψ/∂I2·F·F^t·F - pF
    # minus 项来源于由 P_2 = 0 确定的静水压力 p
    minus  = two * (dPsidI1 * one / K.square(Stretch) + dPsidI2 * one / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2 * one) - minus
    return stress
```

**公式 (15)**：

$$
\boldsymbol{P} = \frac{\partial\psi}{\partial I_1}\frac{\partial I_1}{\partial\boldsymbol{F}} + \frac{\partial\psi}{\partial I_2}\frac{\partial I_2}{\partial\boldsymbol{F}} - p\boldsymbol{F} = 2\!\left[\frac{\partial\psi}{\partial I_1} + I_1\frac{\partial\psi}{\partial I_2}\right]\boldsymbol{F} - 2\frac{\partial\psi}{\partial I_2}\boldsymbol{F}\cdot\boldsymbol{F}^{\mathrm{t}}\cdot\boldsymbol{F} - p\boldsymbol{F}
$$

---

### 公式 (16)：自由能非负性约束

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L51-L68)）：

```python
# RUBBER/Models.py，第 51–68 行
# 通过非负权重约束保证 ψ ≥ 0（非负系数 × 非负激活函数 ≥ 0）
I_1_w11 = keras.layers.Dense(1,
    kernel_initializer=initializer_1,
    kernel_constraint=keras.constraints.NonNeg(),   # 权重 w ≥ 0
    use_bias=False, activation=None, ...)(I1_ref)   # 线性激活 f(x)=x ≥ 0 当 x=[I1-3]≥0

I_1_w21 = keras.layers.Dense(1,
    kernel_initializer=initializer_exp,
    kernel_constraint=keras.constraints.NonNeg(),   # 权重 w ≥ 0
    use_bias=False, activation=activation_Exp, ...)(...) # exp(wx)-1 ≥ 0 当 w≥0
```

**公式 (16)**：

$$
\psi(\boldsymbol{F}) \geq 0 \quad \forall \quad \boldsymbol{F}
$$

---

### 公式 (17)：参考构型无应力条件

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L116-L117)）：

```python
# RUBBER/Models.py，第 116–117 行
# 通过将网络输入从 I1 改为 (I1 - 3) 保证参考构型下 ψ = 0
# F = I 时，I1 = 3，I2 = 3；因此 [I1-3] = 0，[I2-3] = 0
I1_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I1_in)   # [I1 - 3] = 0 at F=I
I2_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I2_in)   # [I2 - 3] = 0 at F=I
```

当 $\boldsymbol{F} = \boldsymbol{I}$ 时，所有网络输入均为零，所有激活函数输出均为零，从而保证 $\psi = 0$，$\boldsymbol{P} = \boldsymbol{0}$。

**公式 (17)**：

$$
\psi(\boldsymbol{F}) \doteq 0 \quad \text{for}\quad \boldsymbol{P}(\boldsymbol{F}) \doteq \mathbf{0} \quad \text{at}\quad \boldsymbol{F} = \boldsymbol{I}
$$

---

### 公式 (18)：极端变形下自由能趋于无穷

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L36-L37)）：

```python
# RUBBER/Models.py，第 36–37 行
# activation_Exp 实现 f(x) = exp(x) - 1
# 当 x → ∞ 时，exp(x) - 1 → ∞；满足增长条件
def activation_Exp(x):
    return 1.0 * (tf.math.exp(x) - 1.0)

# activation_ln 实现 f(x) = -ln(1 - x)
# 当 x → 1 时，-ln(1-x) → ∞；满足压缩极端增长条件
def activation_ln(x):
    return -1.0 * tf.math.log(1.0 - (x))
```

指数型激活函数在拉伸趋于无穷时提供无界增长；对数型激活函数在压缩趋向极限时同样发散，共同满足物理合理的增长条件。

**公式 (18)**：

$$
\psi(\boldsymbol{F}) \to \infty \quad \text{for}\quad J \to 0 \quad \text{or}\quad J \to \infty
$$

---

### 公式 (19)：多凸自由能的分解结构及其导出应力

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L108-L133)）：

```python
# RUBBER/Models.py，第 108–133 行
# StrainEnergyCANN 函数实现 ψ = ψ1(I1) + ψ2(I2) 的解耦架构
def StrainEnergyCANN():
    I1_in = tf.keras.Input(shape=(1,), name='I1')
    I2_in = tf.keras.Input(shape=(1,), name='I2')
    I1_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I1_in)
    I2_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I2_in)
    # I1 子网络（对应 ψ1(I1)）
    I1_out = SingleInvNet4(I1_ref, 0, L2)
    # I2 子网络（对应 ψ2(I2)）
    I2_out = SingleInvNet4(I2_ref, terms, L2)
    # 相加得到 ψ = ψ1 + ψ2
    ALL_I_out = tf.keras.layers.concatenate([I1_out, I2_out], axis=1)
    W_ANN = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                               use_bias=False, activation=None)(ALL_I_out)
    # P = ∂ψ/∂F 通过 myGradient 在 ContinuumMechanicsFramework 中计算
```

**公式 (19)**：

$$
\boldsymbol{P} = \frac{\partial\psi}{\partial\boldsymbol{F}} = \frac{\partial\psi_1}{\partial I_1}\frac{\partial I_1}{\partial\boldsymbol{F}} + \frac{\partial\psi_2}{\partial I_2}\frac{\partial I_2}{\partial\boldsymbol{F}} + \frac{\partial\psi_3}{\partial I_3}\frac{\partial I_3}{\partial\boldsymbol{F}}
$$

---

## 4. Classical Neural Networks（经典神经网络）

---

### 公式 (20)：经典前馈神经网络的层间传播方程

**对应代码**（[RUBBER/Rubber_CANN.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Rubber_CANN.py#L82-L116)）：

```python
# RUBBER/Rubber_CANN.py，第 82–116 行
# Compile_and_fit 以 Keras 的 Dense 层序列体现 z_k = f(w_k z_{k-1} + b_k) 的结构
# 输入 z_0 = F（以主拉伸 λ 向量形式传入），中间层应用激活函数 f(·)，输出 z_{k+1} ≈ P(F)
def Compile_and_fit(model_given, input_train, output_train, epochs, path_checkpoint, sample_weights):
    mse_loss = keras.losses.MeanSquaredError()
    opti1    = tf.optimizers.Adam(learning_rate=0.001)
    model_given.compile(loss=mse_loss, optimizer=opti1, ...)
    history = model_given.fit(
        input_train,    # z_0 = F（输入层）
        output_train,   # 目标 P（输出层）
        batch_size=batch_size, epochs=epochs, ...)

# BCANN/BCANN_main.py，第 225–265 行：明确展示逐层传播
# z_0 = [I1_in, I2_in]（F 的不变量形式）
# z_k = f(w_k · z_{k-1})（每个 NormalizedDense 层对应一次传播）
for ii in range(terms):
    I_1_w11 = NormalizedDense(
        kernel_initializer=init_list[ii],
        activation=activation_list[ii], ...)(Input_list[ii])   # z_k = f(w · z_{k-1})
```

**公式 (20)**：

$$
\begin{array}{rcl}
\mathbf{z}_0 &=& \mathbf{F}\\
\mathbf{z}_1 &=& f(\mathbf{w}_1\mathbf{z}_0 + \mathbf{b}_1)\\
\mathbf{z}_2 &=& f(\mathbf{w}_2\mathbf{z}_1 + \mathbf{b}_2)\\
\mathbf{z}_3 &=& \mathbf{w}_3\mathbf{z}_2 + \mathbf{b}_3 \approx \mathbf{P}(\mathbf{F})
\end{array}
$$

---

### 公式 (21)：经典神经网络的显式展开表达式

**对应代码**（[BCANN/BCANN_main.py](https://github.com/marksweli/CANN/blob/main/BCANN/BCANN_main.py#L100-L162)）：

```python
# BCANN/BCANN_main.py，第 100–120 行
# 使用 tanh 激活函数的经典神经网络
acti1 = 'tanh'

# NormalizedDense.call 方法，第 219–221 行
def call(self, inputs):
    return self.w2 * self.activation(inputs * self.w1)
# 对 tanh 激活：输出 = w2 · tanh(w1 · 输入)
# 对两层网络展开等同于公式 (21) 的嵌套结构：
# z11 = tanh(w111 · F11 + b11)
# z21 = tanh(w211 · z11 + w212 · z12 + b21)
# P11 ≈ w321 · z21 + w322 · z22 + b31
```

**公式 (21)**：

$$
\begin{array}{rcl}
z_0 &=& F_{11}\\
z_{11} &=& \tanh(w_{111}\cdot F_{11} + b_{11})\\
z_{12} &=& \tanh(w_{112}\cdot F_{11} + b_{12})\\
z_{21} &=& \tanh(w_{211}\cdot z_{11} + w_{212}\cdot z_{12} + b_{21})\\
z_{22} &=& \tanh(w_{221}\cdot z_{11} + w_{222}\cdot z_{12} + b_{22})\\
z_3 &=& w_{321}\cdot z_{21} + w_{322}\cdot z_{22} + b_{31}\\
P_{11} &\approx& w_{321}\cdot(\tanh(w_{211}\cdot\tanh(w_{111}\cdot F_{11} + b_{11}) + \tanh(w_{212}\cdot\tanh(w_{112}\cdot F_{11}+b_{12}))+b_{21}))\\
&&+\,w_{322}\cdot(\tanh(w_{221}\cdot\tanh(w_{111}\cdot F_{11}+b_{11})+\tanh(w_{222}\cdot\tanh(w_{112}\cdot F_{11}+b_{12}))+b_{22}))+b_{31}
\end{array}
$$

---

### 公式 (22)：经典神经网络的损失函数（均方误差）

**对应代码**（[RUBBER/Rubber_CANN.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Rubber_CANN.py#L83-L90)）：

```python
# RUBBER/Rubber_CANN.py，第 83–90 行
def Compile_and_fit(model_given, input_train, output_train, epochs, path_checkpoint, sample_weights):
    mse_loss = keras.losses.MeanSquaredError()   # L = (1/n) Σ ||P(Fi) - P̂_i||²
    metrics  = [keras.metrics.MeanSquaredError()]
    opti1    = tf.optimizers.Adam(learning_rate=0.001)
    model_given.compile(loss=mse_loss, optimizer=opti1, metrics=metrics)
```

**公式 (22)**：

$$
L(\boldsymbol{\theta};\boldsymbol{F}) = \frac{1}{n_{\mathrm{trn}}}\sum_{i=1}^{n_{\mathrm{trn}}}\|\boldsymbol{P}(F_i) - \hat{\boldsymbol{P}}_i\|^2 \to \min
$$

---

## 5. Constitutive Artificial Neural Networks（本构人工神经网络）

---

### 公式 (24)：CANN 自由能函数（八项展开）

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L82-L133)）：

```python
# RUBBER/Models.py，第 82–133 行
# SingleInvNet4 对单个不变量构建四项子网络
def SingleInvNet4(I1_ref, idi, L2):
    # 第一阶：线性项 w_{2,1}·w_{1,1}·[I1-3]
    I_1_w11 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                                 use_bias=False, activation=None, ...)( I1_ref)
    # 第一阶：指数项 w_{2,2}·[exp(w_{1,2}·[I1-3]) - 1]
    I_1_w21 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                                 use_bias=False, activation=activation_Exp, ...)(I1_ref)
    # 第二阶：平方线性项 w_{2,3}·w_{1,3}·[I1-3]²
    I_1_w31 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                                 use_bias=False, activation=None, ...)(tf.math.square(I1_ref))
    # 第二阶：平方指数项 w_{2,4}·[exp(w_{1,4}·[I1-3]²) - 1]
    I_1_w41 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                                 use_bias=False, activation=activation_Exp, ...)(tf.math.square(I1_ref))
    collect = [I_1_w11, I_1_w21, I_1_w31, I_1_w41]
    collect_out = tf.keras.layers.concatenate(collect, axis=1)
    return collect_out

def StrainEnergyCANN():
    ...
    I1_out = SingleInvNet4(I1_ref, 0, L2)      # 4 个 I1 相关项
    I2_out = SingleInvNet4(I2_ref, terms, L2)  # 4 个 I2 相关项
    ALL_I_out = tf.keras.layers.concatenate([I1_out, I2_out], axis=1)
    W_ANN = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                               use_bias=False, activation=None)(ALL_I_out)
    # 输出 ψ = Σ w_{2,k} · (第k项激活) = 公式(24)
```

**公式 (24)**：

$$
\begin{aligned}
\psi(I_1,I_2) =\ &w_{2,1}w_{1,1}[I_1-3] + w_{2,2}[\exp(w_{1,2}[I_1-3])-1]\\
&+ w_{2,3}w_{1,3}[I_1-3]^2 + w_{2,4}[\exp(w_{1,4}[I_1-3]^2)-1]\\
&+ w_{2,5}w_{1,5}[I_2-3] + w_{2,6}[\exp(w_{1,6}[I_2-3])-1]\\
&+ w_{2,7}w_{1,7}[I_2-3]^2 + w_{2,8}[\exp(w_{1,8}[I_2-3]^2)-1]
\end{aligned}
$$

---

### 公式 (25)：从 CANN 自由能导出的 Piola 应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L26-L69)）：

```python
# RUBBER/ContinuumMechHelper.py，第 26–69 行
# myGradient 实现自动微分：dψ/dI1, dψ/dI2
def myGradient(a, b):
    der = tf.gradients(a, b, unconnected_gradients='zero')
    return der[0]

# 各加载模式的应力计算
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    two = tf.constant(2.0, dtype='float32')
    minus  = two * (dPsidI1 / K.square(Stretch) + dPsidI2 / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2) - minus
    return stress

def Stress_calc_ET(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    stress = two * (dPsidI1 + K.pow(Stretch,2) * dPsidI2) * (Stretch - 1.0/K.pow(Stretch,5))
    return stress

def Stress_calc_PS(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    two = tf.constant(2.0, dtype='float32')
    minus  = two * (dPsidI1 / K.pow(Stretch, 3) + dPsidI2 / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2 * Stretch) - minus
    return stress
```

**公式 (25)**：

$$
\begin{aligned}
P =\ &\frac{\partial\psi}{\partial I_1}\cdot\frac{\partial I_1}{\partial\boldsymbol{F}} + \frac{\partial\psi}{\partial I_2}\cdot\frac{\partial I_2}{\partial\boldsymbol{F}},\\
P =\ &\Big[w_{2,1}w_{1,1} + w_{2,2}w_{1,2}\exp(w_{1,2}[I_1-3])\\
&\quad+2[I_1-3][w_{2,3}w_{1,3}+w_{2,4}w_{1,4}\exp(w_{1,4}[I_1-3]^2)]\Big]\,\partial I_1/\partial\boldsymbol{F}\\
&+\Big[w_{2,5}w_{1,5}+w_{2,6}w_{1,6}\exp(w_{1,6}[I_2-3])\\
&\quad+2[I_2-3][w_{2,7}w_{1,7}+w_{2,8}w_{1,8}\exp(w_{1,8}[I_2-3]^2)]\Big]\,\partial I_2/\partial\boldsymbol{F}
\end{aligned}
$$

---

### 公式 (26)：neo Hooke 模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L84-L87)）：

```python
# RUBBER/Models.py，第 84–87 行
# neo Hooke 对应 CANN 中 I1 子网络的第一项（线性激活）
# ψ = w_{2,1} · w_{1,1} · [I1-3]，其中 μ = 2 w_{1,1} w_{2,1}
I_1_w11 = keras.layers.Dense(1,
    kernel_initializer='glorot_normal',
    kernel_constraint=keras.constraints.NonNeg(),
    use_bias=False,
    activation=None,          # f(x) = x，对应 ψ1 = w·[I1-3]
    name='w'+str(1+idi)+'1')(I1_ref)
```

**公式 (26)**：

$$
\psi = \tfrac{1}{2}\mu[I_1-3] \qquad \text{where}\qquad \mu = 2w_{1,1}w_{2,1} \text{ in Eq. (24)}
$$

---

### 公式 (27)：Blatz–Ko 模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L84-L101)）：

```python
# RUBBER/Models.py，第 84–101 行
# Blatz-Ko 对应 CANN 中 I2 子网络的第一项（线性激活）
# ψ = w_{2,5} · w_{1,5} · [I2-3]，其中 μ = 2 w_{1,5} w_{2,5}
# 通过将 I2_ref 输入到 SingleInvNet4 的第一个 Dense 层实现
I2_out = SingleInvNet4(I2_ref, terms, L2)
# 其中 SingleInvNet4 的第一个节点：
I_1_w11 = keras.layers.Dense(1,
    kernel_constraint=keras.constraints.NonNeg(),
    use_bias=False,
    activation=None,    # 线性激活：ψ ∝ w·[I2-3]
    name='w'+str(1+idi)+'1')(I1_ref)   # 此处 I1_ref 对应 I2_ref 的调用
```

**公式 (27)**：

$$
\psi = \tfrac{1}{2}\mu[I_2-3] \qquad \text{where}\qquad \mu = 2w_{1,5}w_{2,5} \text{ in Eq. (24)}
$$

---

### 公式 (28)：Mooney–Rivlin 模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L108-L133)）：

```python
# RUBBER/Models.py，第 108–133 行
# Mooney-Rivlin = neo Hooke（I1 线性项）+ Blatz-Ko（I2 线性项）
# 在 StrainEnergyCANN 中，I1 和 I2 子网络各自的第一项同时激活时即为 Mooney-Rivlin
def StrainEnergyCANN():
    ...
    I1_out = SingleInvNet4(I1_ref, 0, L2)          # 含 w_{1,1}·[I1-3] 等项
    I2_out = SingleInvNet4(I2_ref, terms, L2)      # 含 w_{1,5}·[I2-3] 等项
    ALL_I_out = tf.keras.layers.concatenate([I1_out, I2_out], axis=1)
    W_ANN = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                               use_bias=False, activation=None)(ALL_I_out)
    # ψ = μ1/2·[I1-3] + μ2/2·[I2-3]（当仅第一、五节点权重非零时）
```

**公式 (28)**：

$$
\psi = \tfrac{1}{2}\mu_1[I_1-3] + \tfrac{1}{2}\mu_2[I_2-3] \qquad \text{where}\qquad \mu_1 = 2w_{1,1}w_{2,1},\ \mu_2 = 2w_{1,5}w_{2,5} \text{ in Eq. (24)}
$$

---

### 公式 (29)：Yeoh 模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L82-L105)）：

```python
# RUBBER/Models.py，第 82–105 行
# Yeoh 模型需要 I1 子网络的线性项与平方项
# 线性项：activation=None → w·[I1-3]
I_1_w11 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                             use_bias=False, activation=None, ...)(I1_ref)
# 平方线性项：activation=None 作用于 [I1-3]²
I_1_w31 = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                             use_bias=False, activation=None, ...)(tf.math.square(I1_ref))
# 通过第二层权重 W_ANN 合并：ψ = a1·[I1-3] + a2·[I1-3]²
```

**公式 (29)**：

$$
\psi = a_1[I_1-3] + a_2[I_1-3]^2 + a_3[I_1-3]^3 \qquad \text{where}\qquad a_1 = 2w_{1,1}w_{2,1},\ a_2 = 2w_{1,3}w_{2,3},\ a_3 = 0
$$

---

### 公式 (30)：Demiray 模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L36-L37)）：

```python
# RUBBER/Models.py，第 36–37 行
# Demiray 模型对应 I1 子网络的线性指数激活项
# activation_Exp 实现 f(x) = exp(x) - 1
def activation_Exp(x):
    return 1.0 * (tf.math.exp(x) - 1.0)

# 第 88–93 行：I_1_w21 节点
I_1_w21 = keras.layers.Dense(1,
    kernel_initializer=initializer_exp,
    kernel_constraint=keras.constraints.NonNeg(),
    use_bias=False,
    activation=activation_Exp,   # f(w·[I1-3]) = exp(w·[I1-3]) - 1
    name='w'+str(2+idi)+'1')(I1_ref)
# 该节点输出为 w_{2,2}·[exp(w_{1,2}·[I1-3]) - 1]
# Demiray: ψ = (a/2b)·[exp(b[I1-3])-1]，其中 a=2w12·w22, b=w12
```

**公式 (30)**：

$$
\psi = \frac{1}{2}\frac{a}{b}[\exp(b[I_1-3])-1] \qquad \text{where}\qquad a = 2w_{1,2}w_{2,2},\ b = w_{1,2} \text{ in Eq. (24)}
$$

---

### 公式 (31)：Treloar/Mooney–Rivlin 近似不可压缩体积项

**对应代码**（[GCANN/src/CANN/models.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/models.py#L39-L56)）：

```python
# GCANN/src/CANN/models.py，第 39–56 行
# SingleInvNet 函数通用结构可扩展支持 J 项
# 对体积项 ψ̄ = (1/2)κ[J-1]²，可类比为对 J 的平方激活
def SingleInvNet(I1_ref, idi, should_normalize, p, alpha, I1s_max):
    # 线性项（对应 κ·[J-1] 或 a1·[I1-3]）
    I_1_w11 = 1 * NormalizedDense(kernel_initializer=initializer_1, activation=None, ...)(I1_ref)
    # 指数项（可扩展为对 J-1 的激活）
    I_1_w21 = 1 * NormalizedDense(kernel_initializer=initializer_exp, activation=activation_Exp, ...)(I1_ref)
    # 平方线性项：w·[I-ref]²（对应 ψ̄ = (1/2)κ[J-1]² 形式）
    I_1_w41 = 1 * NormalizedDense(kernel_initializer=initializer_1, activation=None, ...)(tf.math.square(I1_ref))
```

**公式 (31)**：

$$
\bar{\psi} = \tfrac{1}{2}\kappa[J-1]^2 \qquad \text{where}\qquad \kappa = 2w_{1,13}w_{2,13}
$$

---

### 公式 (32)：Holzapfel 模型（横观各向同性）

**对应代码**（[GCANN/src/CANN/models.py](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/models.py#L58-L68)）：

```python
# GCANN/src/CANN/models.py，第 58–68 行
# SingleInvNet_I4 专门处理 I4 项，对应 Holzapfel 模型
def SingleInvNet_I4(I1_ref, idi, should_normalize, p, alpha, I1s_max):
    # activation_Exp_I4 实现 f(x) = exp(x) - x - 1（用于 I4 的指数激活）
    I_1_w21 = 1 * NormalizedDense(kernel_initializer=initializer_exp,
                                   activation=activation_Exp_I4, ...)(I1_ref)
    # 平方线性项 w·[I4-1]²
    I_1_w41 = 1 * NormalizedDense(kernel_initializer=initializer_1,
                                   activation=None, ...)(tf.math.square(I1_ref))
    # 平方指数项 w·[exp(w·[I4-1]²) - 1]（对应 Holzapfel 模型的 ψ̄ = (a/2b)[exp(b[I4-1]²)-1]）
    I_1_w51 = 1 * NormalizedDense(kernel_initializer=initializer_exp,
                                   activation=activation_Exp, ...)(tf.math.square(I1_ref))
```

**公式 (32)**：

$$
\bar{\psi} = \frac{1}{2}\frac{a}{b}[\exp(b[I_4-1]^2)-1] \qquad \text{where}\qquad a = 2w_{1,22}w_{2,22},\ b = w_{1,22}
$$

---

### 公式 (33)：CANN 的损失函数（均方误差）

**对应代码**（[RUBBER/Rubber_CANN.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Rubber_CANN.py#L83-L90)）：

```python
# RUBBER/Rubber_CANN.py，第 83–90 行
# CANN 与经典 NN 使用相同的 MSE 损失函数
def Compile_and_fit(model_given, input_train, output_train, epochs, path_checkpoint, sample_weights):
    mse_loss = keras.losses.MeanSquaredError()     # L = (1/n) Σ ||P(Fi) - P̂_i||²
    opti1    = tf.optimizers.Adam(learning_rate=0.001)   # ADAM 优化器
    model_given.compile(loss=mse_loss, optimizer=opti1, metrics=[keras.metrics.MeanSquaredError()])
```

**公式 (33)**：

$$
L(\theta;\boldsymbol{F}) = \frac{1}{n_{\mathrm{trn}}}\sum_{i=1}^{n_{\mathrm{trn}}}\|P(F_i) - \hat{P}_i\|^2 \to \min
$$

---

## 6. Special homogeneous deformation modes（特殊均匀变形模式）

---

### 公式 (34)：主应力—主拉伸关系（含静水压力）

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L54)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–54 行
# Stress_calc_UT 实现 P_i = ∂ψ/∂λ_i - p/λ_i 在单轴拉伸下的计算
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    # minus 项来自静水压力 p（由 P_2 = P_3 = 0 确定）
    minus  = two * (dPsidI1 / K.square(Stretch) + dPsidI2 / K.pow(Stretch, 3))
    # stress = ∂ψ/∂I1·∂I1/∂λ + ∂ψ/∂I2·∂I2/∂λ - p/λ（在主拉伸坐标下）
    stress = two * (dPsidI1 * Stretch + dPsidI2) - minus
    return stress
```

**公式 (34)**：

$$
P_i = \frac{\partial\psi}{\partial\lambda_i} - \frac{1}{\lambda_i}p \qquad \forall\quad i = 1,2,3
$$

---

### 公式 (35)：以不变量表示的主应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L69)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–69 行
# 通过链式法则将 ∂ψ/∂λ 转换为 ∂ψ/∂I1·∂I1/∂λ + ∂ψ/∂I2·∂I2/∂λ

# 单轴拉伸（UT）
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    minus  = two * (dPsidI1 / K.square(Stretch) + dPsidI2 / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2) - minus
    return stress

# 纯剪切（PS）
def Stress_calc_PS(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    minus  = two * (dPsidI1 / K.pow(Stretch, 3) + dPsidI2 / K.pow(Stretch, 3))
    stress = two * (dPsidI1 * Stretch + dPsidI2 * Stretch) - minus
    return stress

# 等双轴拉伸（ET）
def Stress_calc_ET(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    stress = two * (dPsidI1 + K.pow(Stretch,2) * dPsidI2) * (Stretch - 1.0/K.pow(Stretch,5))
    return stress
```

**公式 (35)**：

$$
P_i = \frac{\partial\psi}{\partial I_1}\frac{\partial I_1}{\partial\lambda_i} + \frac{\partial\psi}{\partial I_2}\frac{\partial I_2}{\partial\lambda_i} - \frac{1}{\lambda_i}p \qquad \forall\quad i = 1,2,3
$$

---

### 公式 (36)：单轴拉伸的变形梯度与应力张量

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L75-L81)）：

```python
# RUBBER/ContinuumMechHelper.py，第 75–81 行
# 单轴拉伸：F = diag{λ, λ^{-1/2}, λ^{-1/2}}，P = diag{P1, 0, 0}
# Stretch_UT = λ 即为 F_{11}，其余分量由不可压缩性确定
Stretch_UT = keras.layers.Input(shape=(1,), name='Stretch_UT')
I1_UT = keras.layers.Lambda(lambda x: x**2 + 2.0/x)(Stretch_UT)
# λ² + 2/λ = λ₁² + λ₂² + λ₃² = λ² + λ^{-1} + λ^{-1}（对 UT）

# 应力 P1 由 Stress_calc_UT 计算，P2 = P3 = 0 由 minus 项处理
Stress_UT = keras.layers.Lambda(function=Stress_calc_UT,
                                name='Stress_UT')([dWdI1_UT, dWdI2_UT, Stretch_UT])
```

**公式 (36)**：

$$
\boldsymbol{F} = \mathrm{diag}\{\lambda,\lambda^{-1/2},\lambda^{-1/2}\} \quad \text{and} \quad \boldsymbol{P} = \mathrm{diag}\{P_1,0,0\}
$$

---

### 公式 (37)：单轴拉伸的不变量及其导数

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L80-L81)）：

```python
# RUBBER/ContinuumMechHelper.py，第 80–81 行
# I1 = λ² + 2/λ，I2 = 2λ + 1/λ²（完全不可压缩单轴拉伸）
# ∂I1/∂λ = 2(λ - 1/λ²)，∂I2/∂λ = 2(1 - 1/λ³)（通过 tf.gradients 自动微分）
I1_UT = keras.layers.Lambda(lambda x: x**2   + 2.0/x  )(Stretch_UT)
I2_UT = keras.layers.Lambda(lambda x: 2.0*x  + 1/x**2 )(Stretch_UT)
```

**公式 (37)**：

$$
I_1 = \lambda^2 + \frac{2}{\lambda} \quad \text{and} \quad I_2 = 2\lambda + \frac{1}{\lambda^2} \quad \text{with} \quad \frac{\partial I_1}{\partial\lambda} = 2\!\left[\lambda - \frac{1}{\lambda^2}\right] \quad \text{and} \quad \frac{\partial I_2}{\partial\lambda} = 2\!\left[1 - \frac{1}{\lambda^3}\right]
$$

---

### 公式 (38)：单轴拉伸的静水压力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L37-L39)）：

```python
# RUBBER/ContinuumMechHelper.py，第 37–39 行
# p 由 P2 = P3 = 0 条件确定：
# p = 2/λ · ∂ψ/∂I1 + 2(λ + 1/λ²) · ∂ψ/∂I2
# 在代码中以 minus 项的形式计算（即 P1 的减去项）
minus = two * (dPsidI1 * one / K.square(Stretch) + dPsidI2 * one / K.pow(Stretch, 3))
# 其中：one/K.square(Stretch) = 1/λ²，对应 ∂I1/∂λ₂|_{λ₂=λ^{-1/2}} 的贡献
#        one/K.pow(Stretch,3) = 1/λ³，对应 ∂I2/∂λ₂ 的贡献
```

**公式 (38)**：

$$
p = \frac{2}{\lambda}\frac{\partial\psi}{\partial I_1} + 2\!\left[\lambda + \frac{1}{\lambda^2}\right]\frac{\partial\psi}{\partial I_2}
$$

---

### 公式 (39)：单轴拉伸的名义应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L31-L41)）：

```python
# RUBBER/ContinuumMechHelper.py，第 31–41 行
# P1 = 2(∂ψ/∂I1 + 1/λ · ∂ψ/∂I2)(λ - 1/λ²)
def Stress_calc_UT(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    # minus = 2(∂ψ/∂I1 · 1/λ² + ∂ψ/∂I2 · 1/λ³)
    minus  = two * (dPsidI1 * one / K.square(Stretch) + dPsidI2 * one / K.pow(Stretch, 3))
    # stress = 2(∂ψ/∂I1 · λ + ∂ψ/∂I2 · 1) - minus
    # = 2(∂ψ/∂I1 + 1/λ · ∂ψ/∂I2)(λ - 1/λ²)
    stress = two * (dPsidI1 * Stretch + dPsidI2 * one) - minus
    return stress
```

**公式 (39)**：

$$
P_1 = 2\!\left[\frac{\partial\psi}{\partial I_1} + \frac{1}{\lambda}\frac{\partial\psi}{\partial I_2}\right]\!\left[\lambda - \frac{1}{\lambda^2}\right]
$$

---

### 公式 (40)：等双轴拉伸的变形梯度与应力张量

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L86-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 86–87 行
# 等双轴拉伸：F = diag{λ, λ, λ^{-2}}，P = diag{P1, P1, 0}
# I1 = 2λ² + 1/λ⁴，I2 = λ⁴ + 2/λ²
Stretch_ET = keras.layers.Input(shape=(1,), name='Stretch_ET')
I1_ET = keras.layers.Lambda(lambda x: 2.0*x**2 + 1.0/x**4)(Stretch_ET)
I2_ET = keras.layers.Lambda(lambda x:     x**4 + 2.0/x**2)(Stretch_ET)
Stress_ET = keras.layers.Lambda(function=Stress_calc_ET,
                                name='Stress_ET')([dWdI1_ET, dWdI2_ET, Stretch_ET])
```

**公式 (40)**：

$$
\boldsymbol{F} = \mathrm{diag}\{\lambda,\lambda,\lambda^{-2}\} \quad \text{and} \quad \boldsymbol{P} = \mathrm{diag}\{P_1,P_2,0\}
$$

---

### 公式 (41)：等双轴拉伸的不变量及其导数

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L86-L87)）：

```python
# RUBBER/ContinuumMechHelper.py，第 86–87 行
# I1 = 2λ² + 1/λ⁴，∂I1/∂λ = 2(λ - 1/λ⁵)（自动微分计算）
# I2 = λ⁴ + 2/λ²，∂I2/∂λ = 2(λ³ - 1/λ³)（自动微分计算）
I1_ET = keras.layers.Lambda(lambda x: 2.0*x**2 + 1.0/x**4)(Stretch_ET)
I2_ET = keras.layers.Lambda(lambda x:     x**4 + 2.0/x**2)(Stretch_ET)
```

**公式 (41)**：

$$
I_1 = 2\lambda^2 + \frac{1}{\lambda^4} \quad \text{and} \quad I_2 = \lambda^4 + \frac{2}{\lambda^2} \quad \text{with} \quad \frac{\partial I_1}{\partial\lambda} = 2\!\left[\lambda - \frac{1}{\lambda^5}\right] \quad \text{and} \quad \frac{\partial I_2}{\partial\lambda} = 2\!\left[\lambda^3 - \frac{1}{\lambda^3}\right]
$$

---

### 公式 (42)：等双轴拉伸的静水压力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L57-L68)）：

```python
# RUBBER/ContinuumMechHelper.py，第 57–68 行
# 等双轴拉伸：p 由 P3 = 0 确定
# p = 2/λ⁴ · ∂ψ/∂I1 + 4/λ² · ∂ψ/∂I2
# 代码等价实现：stress = 2(∂ψ/∂I1 + λ²·∂ψ/∂I2)(λ - 1/λ⁵) 已包含压力消去
def Stress_calc_ET(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    # 直接以 P3=0 消去 p 后的最终表达式
    stress = two * (dPsidI1 + K.pow(Stretch, 2) * dPsidI2) * (Stretch - one / K.pow(Stretch, 5))
    return stress
```

**公式 (42)**：

$$
p = \frac{2}{\lambda^4}\frac{\partial\psi}{\partial I_1} + \frac{4}{\lambda^2}\frac{\partial\psi}{\partial I_2}
$$

---

### 公式 (43)：等双轴拉伸的名义应力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L57-L68)）：

```python
# RUBBER/ContinuumMechHelper.py，第 57–68 行
# P1 = P2 = 2(∂ψ/∂I1 + λ²·∂ψ/∂I2)(λ - 1/λ⁵)
def Stress_calc_ET(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    stress = two * (dPsidI1 + K.pow(Stretch, 2) * dPsidI2) * (Stretch - one / K.pow(Stretch, 5))
    return stress
```

**公式 (43)**：

$$
P_1 = P_2 = 2\!\left[\frac{\partial\psi}{\partial I_1} + \lambda^2\frac{\partial\psi}{\partial I_2}\right]\!\left[\lambda - \frac{1}{\lambda^5}\right]
$$

---

### 公式 (44)：纯剪切的变形梯度与应力张量

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L83-L84)）：

```python
# RUBBER/ContinuumMechHelper.py，第 83–84 行
# 纯剪切：F = diag{λ, 1, λ^{-1}}，P = diag{P1, P2, 0}
# I1 = I2 = λ² + 1 + 1/λ²
Stretch_PS = keras.layers.Input(shape=(1,), name='Stretch_PS')
I1_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)
I2_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)
Stress_PS = keras.layers.Lambda(function=Stress_calc_PS,
                                name='Stress_PS')([dWdI1_PS, dWdI2_PS, Stretch_PS])
```

**公式 (44)**：

$$
\boldsymbol{F} = \mathrm{diag}\{\lambda,1,\lambda^{-1}\} \quad \text{and} \quad \boldsymbol{P} = \mathrm{diag}\{P_1,P_2,0\}
$$

---

### 公式 (45)：纯剪切的不变量及其导数

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L83-L84)）：

```python
# RUBBER/ContinuumMechHelper.py，第 83–84 行
# I1 = I2 = λ² + 1 + 1/λ²（纯剪切特有性质：I1 = I2）
# ∂I1/∂λ = ∂I2/∂λ = 2(λ - 1/λ³)（通过 tf.gradients 自动微分）
I1_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)
I2_PS = keras.layers.Lambda(lambda x: x**2 + 1.0 + 1.0/x**2)(Stretch_PS)
# I1 = I2 体现在代码中两个 Lambda 函数完全相同
```

**公式 (45)**：

$$
I_1 = I_2 = \lambda^2 + 1 + \frac{1}{\lambda^2} \quad \text{with} \quad \frac{\partial I_1}{\partial\lambda} = \frac{\partial I_2}{\partial\lambda} = 2\!\left[\lambda - \frac{1}{\lambda^3}\right]
$$

---

### 公式 (46)：纯剪切的静水压力

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L44-L54)）：

```python
# RUBBER/ContinuumMechHelper.py，第 44–54 行
# 纯剪切：p 由 P3 = 0 确定
# p = 2/λ² · ∂ψ/∂I1 + 2(1 + 1/λ²) · ∂ψ/∂I2
# minus 项体现了该压力的贡献
def Stress_calc_PS(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    minus  = two * (dPsidI1 * one / K.pow(Stretch, 3) + dPsidI2 * one / K.pow(Stretch, 3))
    # 1/K.pow(Stretch,3) = 1/λ³，对应 ∂I1/∂λ₃|_{λ₃=λ^{-1}} 的贡献
    stress = two * (dPsidI1 * Stretch + dPsidI2 * Stretch) - minus
    return stress
```

**公式 (46)**：

$$
p = \frac{2}{\lambda^2}\frac{\partial\psi}{\partial I_1} + 2\!\left[1 + \frac{1}{\lambda^2}\right]\frac{\partial\psi}{\partial I_2}
$$

---

### 公式 (47)：纯剪切的名义应力（两个方向）

**对应代码**（[RUBBER/ContinuumMechHelper.py](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py#L44-L54)）：

```python
# RUBBER/ContinuumMechHelper.py，第 44–54 行
# P1 = 2(∂ψ/∂I1 + ∂ψ/∂I2)(λ - 1/λ³)
# P2 = 2(∂ψ/∂I1 + λ²·∂ψ/∂I2)(1 - 1/λ²)（在代码中以 minus 间接实现）
def Stress_calc_PS(inputs):
    (dPsidI1, dPsidI2, Stretch) = inputs
    one = tf.constant(1.0, dtype='float32')
    two = tf.constant(2.0, dtype='float32')
    # minus = 2(∂ψ/∂I1 · 1/λ³ + ∂ψ/∂I2 · 1/λ³)  [来自 P3=0 的压力贡献]
    minus  = two * (dPsidI1 * one / K.pow(Stretch, 3) + dPsidI2 * one / K.pow(Stretch, 3))
    # stress = P1 = 2(∂ψ/∂I1 + ∂ψ/∂I2)(λ - 1/λ³)
    stress = two * (dPsidI1 * Stretch + dPsidI2 * Stretch) - minus
    return stress
```

**公式 (47)**：

$$
P_1 = 2\!\left[\frac{\partial\psi}{\partial I_1} + \frac{\partial\psi}{\partial I_2}\right]\!\left[\lambda - \frac{1}{\lambda^3}\right] \quad \text{and} \quad P_2 = 2\!\left[\frac{\partial\psi}{\partial I_1} + \lambda^2\frac{\partial\psi}{\partial I_2}\right]\!\left[1 - \frac{1}{\lambda^2}\right]
$$

---

## 7. Results（结果）

---

### 公式 (48)：CANN 发现的橡胶（20°C）自由能模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L108-L133)）：

```python
# RUBBER/Models.py，第 108–133 行
# 20°C 下的训练结果：CANN 自主发现三项模型
# ψ = μ1/2·[I1-3] + (a1/2b1)·[exp(b1[I1-3])-1] + (a2/2b2)·[exp(b2[I2-3])-1]
# 对应的网络权重（代码通过 Psi_model.get_weights() 获取）：
# μ1 = 2·w_{1,1}·w_{2,1} = 0.2370 MPa（线性 I1 项）
# a1 = 2·w_{1,2}·w_{2,2} = 0.0582 MPa, b1 = w_{1,2} = 0.0387（线性指数 I1 项）
# a2 = 2·w_{1,6}·w_{2,6} = 0.0013 MPa, b2 = w_{1,6} = 0.0022（线性指数 I2 项）
def StrainEnergyCANN():
    I1_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I1_in)
    I2_ref = keras.layers.Lambda(lambda x: (x - 3.0))(I2_in)
    I1_out = SingleInvNet4(I1_ref, 0, L2)    # 生成 4 个 I1 相关项
    I2_out = SingleInvNet4(I2_ref, terms, L2)  # 生成 4 个 I2 相关项
    # 通过训练自主选择非零权重（多数权重训练至零）
    W_ANN = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                               use_bias=False, activation=None)(ALL_I_out)
```

**公式 (48)**：

$$
\psi(I_1,I_2) = \frac{1}{2}\mu_1[I_1-3] + \frac{1}{2}\frac{a_1}{b_1}[\exp(b_1[I_1-3])-1] + \frac{1}{2}\frac{a_2}{b_2}[\exp(b_2[I_2-3])-1]
$$

其中 $\mu_1 = 0.2370$ MPa，$a_1 = 0.0582$ MPa，$b_1 = 0.0387$，$a_2 = 0.0013$ MPa，$b_2 = 0.0022$。

---

### 公式 (49)：CANN 发现的橡胶（50°C）自由能模型

**对应代码**（[RUBBER/Models.py](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py#L108-L133)）：

```python
# RUBBER/Models.py，第 108–133 行
# 50°C 下的训练结果：CANN 自主发现三项模型
# ψ = μ1/2·[I1-3] + μ2/2·[I2-3] + (a1/2b1)·[exp(b1[I1-3])-1]
# 对应的网络权重（代码通过 Psi_model.get_weights() 获取）：
# μ1 = 2·w_{1,1}·w_{2,1} = 0.2830 MPa（线性 I1 项）
# μ2 = 2·w_{1,5}·w_{2,5} = 0.0141 MPa（线性 I2 项）
# a1 = 2·w_{1,2}·w_{2,2} = 0.0434 MPa, b1 = w_{1,2} = 0.0541（线性指数 I1 项）
def StrainEnergyCANN():
    ...
    I1_out = SingleInvNet4(I1_ref, 0, L2)
    I2_out = SingleInvNet4(I2_ref, terms, L2)
    # 训练后仅 μ1(I1线性项), μ2(I2线性项), a1/b1(I1指数项) 非零
    W_ANN = keras.layers.Dense(1, kernel_constraint=keras.constraints.NonNeg(),
                               use_bias=False, activation=None)(ALL_I_out)

# 权重提取（Rubber_CANN.py 第 232–249 行）
second_layer = Psi_model.get_layer('wx2').get_weights()[0].flatten()
for i in range(1, int(terms + 1)):
    value = Psi_model.get_layer('w' + str(i) + '1').get_weights()[0][0][0]
    weight_matrix[i-1, 0] = value
    weight_matrix[i-1, 1] = second_layer[j]
```

**公式 (49)**：

$$
\psi(I_1,I_2) = \frac{1}{2}\mu_1[I_1-3] + \frac{1}{2}\mu_2[I_2-3] + \frac{1}{2}\frac{a_1}{b_1}[\exp(b_1[I_1-3])-1]
$$

其中 $\mu_1 = 0.2830$ MPa，$\mu_2 = 0.0141$ MPa，$a_1 = 0.0434$ MPa，$b_1 = 0.0541$。

---

## 附录：主要代码文件索引

| 代码文件 | GitHub 链接 | 主要内容 |
|---------|------------|---------|
| `RUBBER/ContinuumMechHelper.py` | [链接](https://github.com/marksweli/CANN/blob/main/RUBBER/ContinuumMechHelper.py) | 应力计算（UT/PS/ET），自动微分，不变量计算 |
| `RUBBER/Models.py` | [链接](https://github.com/marksweli/CANN/blob/main/RUBBER/Models.py) | CANN 网络架构，激活函数，自由能子网络 |
| `RUBBER/Rubber_CANN.py` | [链接](https://github.com/marksweli/CANN/blob/main/RUBBER/Rubber_CANN.py) | 训练循环，损失函数，数据加载 |
| `GCANN/src/CANN/cont_mech.py` | [链接](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/cont_mech.py) | 广义不变量计算，各向异性应力计算 |
| `GCANN/src/CANN/models.py` | [链接](https://github.com/marksweli/CANN/blob/main/GCANN/src/CANN/models.py) | 广义 CANN，横观各向同性模型，Holzapfel 项 |
| `BCANN/BCANN_main.py` | [链接](https://github.com/marksweli/CANN/blob/main/BCANN/BCANN_main.py) | 贝叶斯 CANN，经典 NN 示例，tanh 激活 |
